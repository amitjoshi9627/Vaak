experiment_name: v0.1.1_baseline

model:
  name: wavlm_base
  pretrained: microsoft/wavlm-base

data:
  manifest: data/manifests/train.csv
  sample_rate: 16000
  chunk_duration_seconds: 4.0

training:
  batch_size: 8
  learning_rate: 0.0001
  epochs: 5